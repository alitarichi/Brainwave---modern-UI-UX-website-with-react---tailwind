import Section from "./Section";

const Hero = () => {
  return (
    <Section
      className="pt-[12rem] -mt-[5.25]"
      crosses
      crossOffset="lg:translate-y-[5.25rem]"
      customPaddings
      id="hero"
    >
      <div></div>
    </Section>
  );
};

export default Hero;
